<h1>lista de Post:</h1>
  {{#each posts}}
  <small>{{createdAt}}</small>
  <p><small>{{updatedAt}}</small></p>
    <h2>titulo: {{titulo}}</h2>
    <p>conteudo: {{conteudo}}</p>
    <a href="/deletar/{{id}}"><button>Deletar</button></a>
    <hr>
  {{/each}}
